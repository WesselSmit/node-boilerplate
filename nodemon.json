{
    "_comments": [
        "'kill $(lsof -t -i:3000)' kills the port node uses making it available for when nodemon restarts (see https://github.com/remy/nodemon/issues/1316)",
        "-i:3000 in the 'event' commands should be replaced with your apps <PORT>",
        "delay property is to ensure 'event' commands are executed before restarting nodemon"
    ],
    "events": {
        "restart": "kill $(lsof -t -i:3000)",
        "crash": "kill $(lsof -t -i:3000)"
    },
    "delay": "1000"
}